import { MermaidDiagram } from "@/components/MermaidDiagram";
import { ArtemisChart } from "@/components/ArtemisChart";
import { CodeBlock } from "@/components/CodeBlock";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { Shield, Cpu, Activity, Zap, Server, Search, BarChart3, Lock, ZapOff } from "lucide-react";

export const metadata = {
  title: 'Project Artemis | System Reliability Engine',
  description: 'Benchmarking engine isolating system noise from performance metrics. 99.999% uptime validation.',
}

<div className="space-y-12 pb-24">

  {/* Hero / Premium Header */}
  <div className="relative pt-8 pb-12 border-b border-zinc-900 overflow-hidden">
    <div className="absolute top-0 right-0 -mr-24 -mt-24 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl" />
    <div className="relative z-10 space-y-6">
        <div className="flex items-center space-x-3 text-blue-400 animate-pulse">
            <Shield className="w-6 h-6" />
            <span className="text-sm font-mono tracking-[0.2em] uppercase">Enterprise Grade // Reliability Engine</span>
        </div>
        <div className="space-y-2">
            <h1 className="text-5xl md:text-7xl font-bold tracking-tighter bg-gradient-to-r from-white via-zinc-200 to-blue-500 bg-clip-text text-transparent italic">
                Project Artemis
            </h1>
            <p className="text-2xl font-light text-zinc-400 max-w-2xl leading-relaxed">
                A benchmarking engine built to isolate <span className="text-white">OS jitter</span> and <span className="text-white">hardware noise</span> from true application performance metrics.
            </p>
        </div>
        
        <div className="flex flex-wrap gap-3">
            <Badge variant="terminal" className="bg-blue-500/10 text-blue-400 border-blue-500/20 py-1.5 px-4 text-xs">
                99.999% DETERMINISM
            </Badge>
            <Badge variant="outline" className="border-zinc-800 text-zinc-500 py-1.5 px-4 text-xs font-mono">
                RUST // RDTSC
            </Badge>
            <Badge variant="secondary" className="bg-zinc-900 text-zinc-400 py-1.5 px-4 text-xs">
                SYSTEMS_STABLE
            </Badge>
        </div>

        <div className="pt-4">
            <Button className="bg-blue-600 text-white hover:bg-blue-500 font-bold px-8 h-12 rounded-none group transition-all">
                REQUEST DOCUMENTATION
                <Server className="ml-2 w-4 h-4 group-hover:scale-110 transition-all" />
            </Button>
        </div>
    </div>
  </div>

  {/* Objective Overview */}
  <div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
    <div className="space-y-6">
        <h2 className="text-3xl font-bold tracking-tight">System Noise Isolation</h2>
        <div className="space-y-8">
            <div className="flex gap-4">
                <div className="w-12 h-12 rounded-xl bg-zinc-900 flex items-center justify-center shrink-0 border border-zinc-800">
                    <Zap className="w-6 h-6 text-blue-500" />
                </div>
                <div className="space-y-1">
                    <h3 className="font-bold text-lg">RDTSC Profiling</h3>
                    <p className="text-zinc-400 leading-relaxed">
                        By utilizing the <span className="text-white font-semibold">CPU Time-Stamp Counter</span>, Artemis captures nanosecond-level execution data without the overhead of standard kernel-space timers.
                    </p>
                </div>
            </div>
            <div className="flex gap-4">
                <div className="w-12 h-12 rounded-xl bg-zinc-900 flex items-center justify-center shrink-0 border border-zinc-800">
                    <Search className="w-6 h-6 text-blue-500" />
                </div>
                <div className="space-y-1">
                    <h3 className="font-bold text-lg">Anomaly Attribution</h3>
                    <p className="text-zinc-400 leading-relaxed">
                        Artemis distinguishes between network-induced jitter and code-path inefficiency. This ensures financial researchers optimize the <span className="text-white font-semibold">true bottleneck</span>, not system noise.
                    </p>
                </div>
            </div>
        </div>
    </div>
    <div className="relative rounded-2xl border border-zinc-800 bg-zinc-900/30 p-8 backdrop-blur-xl">
        <div className="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-blue-500 to-transparent" />
        <div className="space-y-4">
            <div className="flex justify-between items-center text-sm font-mono text-zinc-500 uppercase tracking-widest">
                <span>Precision Metrics</span>
                <span className="text-blue-400 tracking-normal">&lt; 1ns variance</span>
            </div>
            <div className="space-y-2">
                <div className="h-2 w-full bg-zinc-800 rounded-full overflow-hidden">
                    <div className="h-full w-[98%] bg-blue-500 shadow-[0_0_10px_#3b82f6]" />
                </div>
                <div className="flex justify-between text-[10px] font-mono text-zinc-500 uppercase">
                    <span>Determinism Score</span>
                    <span>98.2%</span>
                </div>
            </div>
            <div className="space-y-2">
                <div className="h-2 w-full bg-zinc-800 rounded-full overflow-hidden">
                    <div className="h-full w-[94%] bg-cyan-400 shadow-[0_0_10px_#22d3ee]" />
                </div>
                <div className="flex justify-between text-[10px] font-mono text-zinc-500 uppercase">
                    <span>Baseline Stability</span>
                    <span>94.5%</span>
                </div>
            </div>
        </div>
        <div className="mt-8 pt-8 border-t border-zinc-800 text-center">
             <div className="text-4xl font-bold text-white tabular-nums">99.999%</div>
             <p className="text-xs text-zinc-500 uppercase tracking-tighter mt-1">Uptime Validation Accuracy</p>
        </div>
    </div>
  </div>

  {/* Jitter Visualization Section */}
  <div className="space-y-6">
    <div className="flex flex-col md:flex-row md:items-end justify-between gap-4">
        <h2 className="text-3xl font-bold tracking-tight">Jitter Analysis</h2>
        <p className="text-zinc-400 text-sm max-w-sm">
            Comparison of Artemis-normalized latency vs default OS measurement. Artemis filters out interrupts and context switches.
        </p>
    </div>
    <ArtemisChart />
  </div>

  {/* Technical Architecture */}
  <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
     <div className="lg:col-span-2 space-y-6">
        <h2 className="text-2xl font-bold flex items-center gap-2">
            <Activity className="w-5 h-5 text-blue-500" /> Profiling Pipeline
        </h2>
        <div className="p-8 rounded-2xl border border-zinc-800 bg-zinc-900/20">
            <MermaidDiagram chart={`
            graph LR
                A[Start RDTSC] --> B{Application Logic}
                B --> C[Stop RDTSC]
                C --> D[Filter OS Noise]
                D --> E[Store in Postgres]
                E --> F[Anomaly Detection]
                
                style A fill:#09090b,stroke:#27272a,color:#fff
                style B fill:#3b82f6,stroke:#3b82f6,color:#fff
                style C fill:#09090b,stroke:#27272a,color:#fff
                style D fill:#3b82f6,stroke:#3b82f6,color:#fff
                style E fill:#09090b,stroke:#27272a,color:#fff
                style F fill:#3b82f6,stroke:#3b82f6,color:#fff
            `} />
        </div>
     </div>
     <div className="space-y-6">
        <h2 className="text-2xl font-bold flex items-center gap-2">
            <Cpu className="w-5 h-5 text-blue-500" /> Rust Profiler
        </h2>
        <CodeBlock
          title="PROFILER.RS"
          highLevel={`
// Isolated benchmarking
fn profile_isolated() {
    let _pin = Thread::current().pin_to_core(1);
    let start = rdtsc::now();
    execute_payload();
    let delta = rdtsc::now() - start;
}
          `}
          lowLevel={`
// Native intrinsic usage
use std::arch::x86_64::_rdtsc;

#[inline(always)]
unsafe fn fast_rdtsc() -> u64 {
    core::arch::x86_64::_mm_lfence();
    let cycles = _rdtsc();
    core::arch::x86_64::_mm_lfence();
    cycles
}
          `}
        />
     </div>
  </div>

</div>
